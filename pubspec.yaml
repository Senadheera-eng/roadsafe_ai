name: road_safe_ai
description: A driver drowsiness detection app

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3
  
  # UI
  google_fonts: ^6.1.0
  
  # Icons
  cupertino_icons: ^1.0.2
  network_info_plus: ^7.0.0
  image: ^4.5.4
  vibration: ^3.1.3
  audioplayers: ^6.5.1
  flutter_local_notifications: ^19.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true